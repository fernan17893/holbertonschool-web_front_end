CREATE TABLE tyrell_corp.nexus6(
    id int NOT NULL,
    name varchar(255) NOT NULL,
);

projectcorrection280hbtn

'holberton_user'@'localhost'

GRANT SELECT ON tyrell_corp.nexus6 TO 'holberton_user'@'localhost';

INSERT INTO tyrell_corp.nexus6 (id,name)
VALUES (1,'Leon');


CREATE USER 'replica_user'@'54.80.195.76' IDENTIFIED WITH mysql_native_password BY 'root';
GRANT REPLICATION SLAVE ON *.* TO 'replica_user'@'54.80.195.76';

 mysql-bin.000001 |      1141


CHANGE REPLICATION SOURCE TO
    SOURCE_HOST='%',
    SOURCE_USER='replica_user',
    SOURCE_PASSWORD='root',
    SOURCE_LOG_FILE='mysql-bin.000001',
    SOURCE_LOG_POS=785;




CHANGE MASTER TO
MASTER_HOST='3.89.29.78',
MASTER_USER='replica_user',
MASTER_PASSWORD='root',
MASTER_LOG_FILE='mysql-bin.000001',
MASTER_LOG_POS=1546;




CHANGE REPLICATION SOURCE TO SOURCE_HOST = '%',SOURCE_USER = 'replica_user',SOURCE_PASSWORD = 'root',SOURCE_LOG_FILE = 'mysql-bin.000001',SOURCE_LOG_POS = 785;